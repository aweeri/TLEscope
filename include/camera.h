#include "raylib.h"
#include <raymath.h>

// Updates the camera position, rotation, zoom, and panning based on mouse input.
void UpdateCameraController(Camera3D* camera, float* angleX, float* angleY, float* distance);
